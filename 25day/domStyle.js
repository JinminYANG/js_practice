// DOM 요소의 스타일 변경

let str = document.getElementById("text");	// 아이디가 "str"인 요소를 선택한다

function changeRedColor() {
    str.style.color = "red";    // 해당 요소의 글자색을 빨간색으로 변경한다
}

function changeBlackColor() {
    str.style.color = "black";  // 해당 요소의 글자색을 검정색으로 변경한다
}