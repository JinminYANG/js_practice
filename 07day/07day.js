// addNum이라는 이름의 함수를 정의
function addNum(x, y) { // x,y는 addNum 함수의 매개변수
    return x + y;
}

let sum = addNum(6,13); // 함수 addNum()을 호출하면서, 인수로 6과 13을 전달한다.
                              // 함수 호출이 끝난 뒤에는 그 반환값을 변수 sum에 대입한다.

console.log(sum); // sum 출력

