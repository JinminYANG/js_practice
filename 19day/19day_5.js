let numObj = new Number(123);   // 123의 값을 가지는 Number 인스턴스를 생성
console.log( typeof numObj );
let num = numObj.valueOf();    // numObj가 가지고있는 값을 반환 (Number형 인스턴스)
console.log( num );
console.log( typeof num );

