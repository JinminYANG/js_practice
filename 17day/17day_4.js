let x = 10 / 0;            // 숫자를 0으로 나누면 Infinity를 반환한다
let y = Infinity * 1000;   // Infinity에 어떠한 수를 산술 연산해도 Infinity를 반환한다
let z = 1 / Infinity;      // Infinity의 역수는 0을 반환한다

console.log(x);
console.log(y);
console.log(z);



