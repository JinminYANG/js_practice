// Array.prototype - sort()

let strArr = ["로마", "나라", "감자", "다람쥐"]; // 한글은 ㄱ,ㄴ,ㄷ순으로 정렬된다.
console.log( strArr.sort() );

let numArr = [10, 21, 1, 2, 3];  // 숫자는 각 자릿수 별로 비교된 후 정렬된다.
console.log( numArr.sort() );

